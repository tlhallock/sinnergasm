apt install rustup build-essential podman net-tools protobuf-compiler libpango1.0-dev libxss-dev libx11-dev libxtst-dev apt-file libgtk-3-dev

apt-file update

sudo snap install rustup --classic
sudo snap install zellij --classic
sudo snap install helix --classic




rustup update
rustup install nightly
rustup target/install x86_64-unknown-linux-musl


sudo mkdir /work
sudo chown rever /work/
git clone https://github.com/tlhallock/sinnergasm.git
apt-file
